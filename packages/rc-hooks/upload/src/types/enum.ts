export const enum FileStatus {
    INIT = 'INIT',
    LOADING = 'LOADING',
    DONE = 'DONE',
    FAIL = 'FAIL',
    ABORT = 'ABORT',
}

export const enum FileType {
    IMAGE = 'image',
    VIDEO = 'video',
    AUDIO = 'audio',
    FILE = 'file',
}
