# Portal 一 传送门

## API
属性 | 说明 | 类型 | 默认值
-----|-----|-----|------
getContainer | 获取父容器的方法，可选，如果不传默认是`document.body` | function | -
children | 传送门内容 | ReactNode | -
onUpdated | updated方法回调，用于准确获取传送门内的dom元素 | () => void | -

